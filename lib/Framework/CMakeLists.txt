#set(FRAMEWORK_LIB_SOURCES
#)

include_directories("${PROJECT_SOURCE_DIR}/lib")
#link_directories(${LIBRARY_DIRS})
add_definitions("-std=c++11")

#add_library(Framework SHARED ${FRAMEWORK_LIB_SOURCES})

#target_link_libraries(Core ...)
